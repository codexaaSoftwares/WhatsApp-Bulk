import apiClient from '../config/apiClient'
import { handleApiError } from '../utils/errorHandler'

const dashboardService = {
  // Dashboard service methods will be added here as needed
}

export default dashboardService
